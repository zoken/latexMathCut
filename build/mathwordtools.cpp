#include "mathwordtools.h"
#include <stdlib.h>
#include "string.h"

void MathWordTool::cut(const char* sentence, Result* result){
    int len = strlen(sentence);
    for(int i = 0 ; i < len ;i++){
        
    }
}
